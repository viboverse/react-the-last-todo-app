class People {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  printInfo() {
    console.log(`The name is ${this.name} and is ${this.age}`);
  }
}

const foo = [1, 2, 3, 4, 5];
foo.push(2);

cal;

const vahab = new People("vahab", 30);

const bar = 12;

vahab.printInfo();
